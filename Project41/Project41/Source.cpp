#include<iostream>
#include<vector>

using namespace std;
class solution
{
public:
	int remove_duplicate(vector<int> m) {
		for (int i=0; i<m.size;i++)
			if (i)
	}
};

int main() {
	vector<int> m;
	solution sl2;
	sl2.remove_duplicate(m);
	for (auto i : m) {
		cout << i << endl;
	}
}